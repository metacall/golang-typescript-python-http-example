const { CasperClient, Keys } = require('casper-js-sdk');

export function deploy_transaction_ts(transferId: number, transferAmount: number): number {
    console.log(CasperClient, Keys);
    return 67;
}
